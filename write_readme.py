content = """# Курсовая работа по ТФА (Вариант 18)

## Описание задания
Вариант 18 соответствует коду задания **231123**.

### Грамматика языка

**1. Типы данных (4-я цифра: 1):**
- `%` - Целый (Integer)
- `!` - Действительный (Real)
- `$` - Логический (Boolean)

**2. Комментарии (6-я цифра: 3):**
- Многострочные комментарии: `(* ... *)`

**3. Структура программы (2-я цифра: 3):**
- Программа состоит из последовательности описаний и операторов.
- Разделители: символ `:` или перевод строки.
- Программа заканчивается ключевым словом `end`.

**4. Описание переменных (3-я цифра: 1):**
- Синтаксис: `идентификатор {, идентификатор } : тип ;`
- Пример: `a, b : %;`

**5. Операторы (5-я цифра: 2):**
- **Присваивание:** `идентификатор := выражение`
- **Составной оператор:** `begin оператор { ; оператор } end`
- **Условный оператор:** `if (выражение) оператор [else оператор]`
- **Цикл с параметром:** `for идентификатор := выражение to выражение [step выражение] оператор next`
- **Цикл пока:** `while (выражение) оператор`
- **Ввод:** `readln идентификатор {, идентификатор }`
- **Вывод:** `writeln выражение {, выражение }`

**6. Операции (1-я цифра: 2):**
- **Отношения:** `!=`, `==`, `<`, `<=`, `>`, `>=`
- **Сложение:** `+`, `-`, `||` (дизъюнкция)
- **Умножение:** `*`, `/`, `&&` (конъюнкция)
- **Унарные:** `!` (отрицание)

### Реализация

Проект состоит из следующих файлов:
- `main.py`: Точка входа. Запускает лексический, синтаксический анализ и интерпретацию.
- `lexer.py`: Лексический анализатор. Разбивает входной текст на токены.
- `parser.py`: Синтаксический анализатор. Строит абстрактное синтаксическое дерево (AST).
- `interpreter.py`: Интерпретатор. Выполняет программу, обходя AST, и проводит семантический контроль типов.
- `program.txt`: Пример программы на модельном языке.

### Запуск

Для запуска программы выполните команду:

```bash
python main.py [имя_файла]
```

Если имя файла не указано, будет использован `program.txt`.

### Пример программы

```
(* Объявление переменных *)
a, b : %;
x : !;

(* Присваивание *)
a := 5
b := 10
x := 3.14

(* Условный оператор *)
if (a < b)
    writeln a
else
    writeln b

(* Цикл *)
for a := 1 to 5 step 1
    writeln a
next

end
```
"""

with open("README.md", "w", encoding="utf-8") as f:
    f.write(content)
